BOT_TOKEN=
OPENROUTER_API_KEY=
# Ваш числовой Telegram user_id (можно узнать у @userinfobot)
OWNER_ID=
DB_PATH=data/bot.db
OPENROUTER_MODEL=nvidia/nemotron-3-super-120b-a12b:free
# Запасная модель (прошла бенчмарк 4/4): minimax/minimax-m2.7:free
RATE_LIMIT=10
RATE_WINDOW=300
# Только для вебхуков (PythonAnywhere и т.п.): случайная строка, например
# python -c "import secrets; print(secrets.token_hex(32))"
TELEGRAM_WEBHOOK_SECRET=
# Прокси исходящих запросов — нужен на PythonAnywhere Free:
# http://<логин>:<API-токен>@proxy.pythonanywhere.com:3128
PROXY_URL=
